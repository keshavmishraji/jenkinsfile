pipeline {
  agent any
  environment {
    USERNAME = 'keshav'
    }
  stages {
    stage('Build') {
      environment {
        API_KEY = 'ISCP123'
        }
      steps {
        sh 'ls'
        sh 'printenv'
        sh 'pwd'
       }
     }
    stage('deploy') {
      steps{
        print "hello world"
       }
      }
     }
    }
